language: 
  - c
  - android

matrix:
    include:
        - env: test="x64 4.8.4 (cmake)"
          language: c
          os: linux
          dist: trusty
          compiler: gcc
          before_script:
              - mkdir build1
              - cd build1
              - cmake ..
          script:  make

        - env: test="x64 5.0 (cmake)"
          language: c
          os: linux
          dist: trusty
          compiler: clang
          before_script:
              - mkdir build2
              - cd build2
              - cmake ..
          script:  make

        - env: test="x64 5.0 (cmake ninja)"
          language: c
          os: linux
          dist: trusty
          compiler: clang
          before_script:
              - mkdir build3
              - cd build3
              - cmake .. -GNinja
          script:  ninja

        - env: test="x64 5.0 (cmake android-15)"
          language: android
          os: linux
          dist: trusty
          compiler: clang
          sudo: true
          install: 
              - echo y | sdkmanager "ndk-bundle"
          before_script:
              - export ANDROID_NDK_HOME=$ANDROID_HOME/ndk-bundle
              - mkdir build4
              - cd build4
              - CC=clang cmake -DCMAKE_TOOLCHAIN_FILE=$ANDROID_HOME/ndk-bundle/build/cmake/android.toolchain.cmake -DANDROID_PLATFORM=android-15 -DANDROID_ABI=armeabi-v7a ..
          script:  make

addons:
    apt:
        packages:
            - cmake
            - ninja-build
